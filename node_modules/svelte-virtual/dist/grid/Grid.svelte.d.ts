export type Item = {
    index: number;
    rowIndex: number;
    columnIndex: number;
    style: string;
};
import type { GetKey, OnScroll, ScrollBehavior } from "../types";
import type { Snippet } from "svelte";
/**
 * - Usage:
 *
 * ```svelte
 * <script>
 * 	import { Grid } from "svelte-virtual";
 *
 * 	let items = [...];
 * </script>
 *
 * <Grid itemCount={items.length} itemHeight={50} itemWidth={60} height={500}>
 * 	{#snippet item({ index, style })}
 * 		<div {style}>
 * 			{items[index]}
 * 		</div>
 * 	{/snippet}
 * </Grid>
 * ```
 */
declare const Grid: import("svelte").Component<{
    itemCount: number;
    itemHeight: number;
    itemWidth: number;
    height: number;
    /**
     * @default "100%"
     */
    width?: string;
    /**
     * @default 1
     */
    overScan?: number;
    /**
     * @default 0
     */
    marginLeft?: number;
    /**
     * @default 0
     */
    marginTop?: number;
    /**
     * @default 0
     */
    scrollPosition?: number;
    /**
     * @default "auto"
     */
    scrollBehavior?: ScrollBehavior;
    /**
     * @default
     * ```ts
     * (index: number) => index
     * ```
     */
    getKey?: GetKey;
    columnCount?: number;
    onscroll?: OnScroll;
    header?: Snippet;
    item: Snippet<[Item]>;
    placeholder?: Snippet<[Item]>;
    footer?: Snippet;
} & Omit<import("svelte/elements").HTMLAttributes<HTMLDivElement>, "placeholder">, {
    scrollToIndex: (index: number, behavior?: ScrollBehavior) => void;
    scrollToPosition: (position: number, behavior?: ScrollBehavior) => void;
}, "scrollPosition">;
export default Grid;
